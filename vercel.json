{
  "buildCommand": "mkdir -p public api && cd backend && npm install && npm run build",
  "installCommand": "cd backend && npm install",
  "framework": null,
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server.js"
    }
  ]
}
