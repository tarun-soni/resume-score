{
  "buildCommand": "mkdir -p public api && if [ -d backend ]; then cd backend && npm install && npm run build; else echo 'Backend folder not found, skipping build'; fi",
  "installCommand": "if [ -d backend ]; then cd backend && npm install; else echo 'Backend folder not found, skipping install'; fi",
  "framework": null,
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server.js"
    }
  ]
}
